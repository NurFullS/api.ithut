spring:
  application:
    name: ithut

  datasource:
    url: jdbc:postgresql://localhost:5432/ithut_db
    username: postgres
    password: Nursultan2010_
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8080